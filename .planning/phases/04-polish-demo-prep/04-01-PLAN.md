---
phase: 04-polish-demo-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/ui/DemoToggle.tsx
  - client/src/components/ui/HoverTooltip.tsx
  - client/src/components/ui/ThermometerHUD.tsx
  - client/src/components/ui/QuizCard.tsx
  - client/src/components/canvas/nodes/SnowballNode.tsx
  - client/src/styles/globals.css
autonomous: true

must_haves:
  truths:
    - "All UI cards (QuizCard, DemoToggle, HoverTooltip, ThermometerHUD) render with consistent frosted glass styling"
    - "Loading states show a custom snowflake spinner animation instead of generic pulse"
    - "Frosted glass uses backdrop-blur-md with slate-900/70 background across all UI cards"
  artifacts:
    - path: "client/src/styles/globals.css"
      provides: "Snowflake spinner keyframe animation and frosted-glass utility class"
      contains: "@keyframes snowflake-spin"
    - path: "client/src/components/ui/DemoToggle.tsx"
      provides: "Frosted glass styled demo toggle"
      contains: "backdrop-blur"
    - path: "client/src/components/ui/HoverTooltip.tsx"
      provides: "Frosted glass styled tooltip"
      contains: "backdrop-blur"
    - path: "client/src/components/ui/ThermometerHUD.tsx"
      provides: "Frosted glass styled thermometer"
      contains: "backdrop-blur"
  key_links:
    - from: "client/src/styles/globals.css"
      to: "client/src/components/canvas/nodes/SnowballNode.tsx"
      via: "snowflake-spin CSS class used in expanding state"
      pattern: "snowflake-spin"
---

<objective>
Apply consistent frosted glass styling to all UI cards and replace generic loading animations with a custom snowflake spinner.

Purpose: Visual polish for "Best UI/UX" award — frosted glass treatment creates cohesive premium feel. Custom loading animation reinforces the winter theme in every interaction.
Output: All UI components with frosted glass + custom snowflake loading animation
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/styles/globals.css
@client/src/components/ui/DemoToggle.tsx
@client/src/components/ui/HoverTooltip.tsx
@client/src/components/ui/ThermometerHUD.tsx
@client/src/components/ui/QuizCard.tsx
@client/src/components/canvas/nodes/SnowballNode.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frosted glass CSS and snowflake spinner to globals.css</name>
  <files>client/src/styles/globals.css</files>
  <action>
Add to the existing `@layer base` block in globals.css:

1. A `@keyframes snowflake-spin` animation that rotates a snowflake icon 360deg over 1.5s with a gentle scale pulse (1 -> 1.15 -> 1 during rotation). Use `ease-in-out` timing.

2. An `.animate-snowflake-spin` utility class applying the keyframe with `infinite` iteration.

3. A `.frosted-glass` utility class with these properties:
   - `background: rgba(15, 23, 42, 0.7)` (slate-900 at 70% opacity)
   - `backdrop-filter: blur(12px)`
   - `-webkit-backdrop-filter: blur(12px)` (Safari support)
   - `border: 1px solid rgba(148, 163, 184, 0.3)` (slate-400/30)
   - `box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3), inset 0 1px 0 rgba(255, 255, 255, 0.05)` (depth + inner highlight)

Keep all existing keyframes (scale-correct, shake) and classes intact.
  </action>
  <verify>Run `cd /Users/sahilpatel/Developer/tidalhack-2026/client && npx tsc --noEmit` to verify no type errors. Visually confirm the CSS file has the new keyframes and classes alongside existing ones.</verify>
  <done>globals.css contains snowflake-spin keyframe, animate-snowflake-spin class, and frosted-glass utility class alongside all existing animations</done>
</task>

<task type="auto">
  <name>Task 2: Apply frosted glass to all UI components and snowflake spinner to loading states</name>
  <files>
    client/src/components/ui/DemoToggle.tsx
    client/src/components/ui/HoverTooltip.tsx
    client/src/components/ui/ThermometerHUD.tsx
    client/src/components/ui/QuizCard.tsx
    client/src/components/canvas/nodes/SnowballNode.tsx
  </files>
  <action>
**DemoToggle.tsx:**
Replace the outer div className from `bg-slate-800/60 backdrop-blur-sm rounded-lg` to use the frosted-glass class: `frosted-glass rounded-lg px-3 py-1.5 flex items-center gap-2`. Remove the old bg/backdrop classes since frosted-glass handles them via CSS.

**HoverTooltip.tsx:**
Replace the inner div className `bg-slate-800/95 ... backdrop-blur-sm border border-slate-700/50` with `frosted-glass text-frost text-xs rounded-lg px-3 py-2 max-w-[250px] shadow-lg`. Keep text-frost and sizing classes.
Also update the tooltip arrow div's `bg-slate-800/95 border-l border-t border-slate-700/50` to use matching colors: `bg-slate-900/70 border-l border-t border-slate-400/30`.

**ThermometerHUD.tsx:**
Replace the thermometer container div className `bg-slate-800/80 backdrop-blur-sm` with `frosted-glass` while keeping `rounded-full border border-slate-600/50 overflow-hidden flex flex-col-reverse`. Actually, since frosted-glass sets its own border, use: `frosted-glass rounded-full overflow-hidden flex flex-col-reverse`. Keep the existing w-8, h-48 sizing on the parent.

**QuizCard.tsx:**
The inner card div already has `bg-slate-900/90 backdrop-blur-md border border-slate-600/50 rounded-2xl p-8 shadow-2xl`. Replace with `frosted-glass rounded-2xl p-8`. The frosted-glass class handles bg, backdrop-blur, border, and shadow. This ensures consistency with all other cards.

**SnowballNode.tsx:**
Replace the `animate-pulse` class used when `isThisNodeExpanding` is true with `animate-snowflake-spin`. This is in the main div className conditional: change `${isThisNodeExpanding ? 'animate-pulse' : ''}` to `${isThisNodeExpanding ? 'animate-snowflake-spin' : ''}`.

Also in HoverTooltip.tsx: replace the loading state `<span className="animate-pulse text-frost/60">Loading...</span>` with a snowflake spinner: `<span className="inline-block animate-snowflake-spin text-frost/60 text-base">&#10052;</span>` (Unicode snowflake character U+2744). Remove the "Loading..." text since the spinning snowflake is self-explanatory.

Do NOT change any component logic, state management, or event handlers. Only className/style changes.
  </action>
  <verify>Run `cd /Users/sahilpatel/Developer/tidalhack-2026/client && npx tsc --noEmit` to verify no type errors. Run `cd /Users/sahilpatel/Developer/tidalhack-2026/client && npm run build` to verify the build succeeds. Visually confirm each file has frosted-glass class applied.</verify>
  <done>All 4 UI cards use consistent frosted-glass styling. SnowballNode expanding state uses snowflake spinner instead of pulse. HoverTooltip loading state shows spinning snowflake instead of pulsing text. Build passes with zero errors.</done>
</task>

</tasks>

<verification>
1. `cd /Users/sahilpatel/Developer/tidalhack-2026/client && npm run build` completes without errors
2. globals.css contains `@keyframes snowflake-spin`, `.animate-snowflake-spin`, and `.frosted-glass`
3. All 4 UI components (DemoToggle, HoverTooltip, ThermometerHUD, QuizCard) reference frosted-glass
4. SnowballNode uses animate-snowflake-spin instead of animate-pulse for expanding state
5. HoverTooltip uses snowflake spinner for loading state
</verification>

<success_criteria>
- All UI cards render with consistent frosted glass (backdrop-blur-md, slate-900/70 bg, subtle border)
- Loading states show spinning snowflake instead of generic pulse
- Build passes with zero errors
- No component logic or state management changed
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-demo-prep/04-01-SUMMARY.md`
</output>
